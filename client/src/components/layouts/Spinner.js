import React from 'react'
import spinner from './Spinner.gif';
const Spinner = () => {
  return (
    <img
        className="loader" 
        alt="LOADER"
        src={spinner}
   />
  )
}

export default Spinner